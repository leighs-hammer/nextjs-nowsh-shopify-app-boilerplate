import React from "react"
export interface IDasboardProps {}

const Dasboard: React.FC<IDasboardProps> = (props): React.ReactElement => {
    return (
      <div>
      // HERE IS A DASHBOARD
      It is minus authentication checks, and state management, use what you choose and wire it up.
      Probaly using a _app HOC
      </div>
    );
}

export default Dasboard;